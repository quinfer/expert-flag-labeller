### Push to QUB GitLab

```bash
cd MSc-Themed-Research-Project/qub-gitlab-submission
git init -b main
git add .
git commit -m "QUB submission: code, docs, CI"
# Replace URL below with your QUB GitLab repository
git remote add origin <GITLAB_REPO_URL>
git push -u origin main
```
